Everything outstanding, in one zip. No SQL — 44, 45 and 46 have all been run.

THE SIMULATION PDF IS NOT HERE BECAUSE IT IS ALREADY ON MAIN
SimulationPrint.jsx, its wiring in Simulation.jsx and the 23 print rules in
index.css are all deployed. There is nothing left to send for it — it did not
appear in the diff because there is no difference.

════════════════════════════════════════════════════════════════════
WHAT IS IN HERE
════════════════════════════════════════════════════════════════════
src/components/Layout.jsx
    The guest nav list had only Dashboard and Transactions. The budgets ROUTE
    was opened to guests last round but nothing pointed at it, so the page
    existed with no way in. This is the only file needed to make guest budgets
    work — migration 44 is already applied.

src/lib/export.js
  · "By category" split across LINES rather than reading the transaction
    header. A split purchase has no category of its own, so every one landed
    under "—" — which was the whole sheet, one unnamed row holding ₪37,603.64.
    The totals reconcile exactly either way: nothing double-counted, nothing
    dropped.
  · The Payer column is omitted for non-mentors instead of exported as a
    column of '***'. The database already masked the name, so this was noise
    rather than a leak — but a column of asterisks takes width and answers
    nothing.

src/pages/Transactions.jsx
  · Passes the split lines to the export, and canSeePayer.
  · The "download all receipts" button is hidden from guests. The receipt
    column was already hidden from them on screen, so the bulk download was a
    way around exactly what that hiding was for — and a receipt carries names,
    addresses and card digits, more than the row it belongs to.

scripts/
    Two new checks, each proven by sabotage: the export must not write an
    unguarded Payer column, and aggregate must preserve the total when it
    splits across lines.

After deploying, index.html should reference index-BudnHXX4.js.
