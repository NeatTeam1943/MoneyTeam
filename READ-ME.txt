Against main @ 1234 (2026-07-29 17:27)

SQL — run in Supabase, in this order:
    26_propose_rls.sql              REQUIRED. Without it a student pressing
                                    "הצע הוצאה" gets
                                    "new row violates row-level security policy"
    24_merge_program_categories.sql merges the 8 FRC/FTC categories into their
                                    parents (21 -> 13). Optional but recommended.
                                    Writes the program onto rows that only said
                                    "shared", so nothing is lost.

14_import_2027_shopping.sql is included only because it is still missing from
the repo. You already ran it. Do not run it again.

CODE — 4 files:
    src/pages/Transactions.jsx  category filter reads the LINE's category, so
                                filtering by a subcategory works
    src/pages/Shopping.jsx      same fix in the actual-vs-requested chart
    src/pages/Budgets.jsx       edit/delete moved onto each part of a split
                                budget; the grouped row is synthetic and its
                                buttons pointed at an id that does not exist
    src/lib/i18n.jsx            one new string for the above

Not included: 25_refresh_transactions_view.sql and src/index.css — main already
has both (they differed only by a trailing newline).

Deploy SQL before code.
