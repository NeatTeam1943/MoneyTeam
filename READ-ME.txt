The Neat mode button was never in the page. Your HTML proved it: EN, the badge
and התנתקות are there and nothing else.

WHAT HAPPENED
I added the import and `const theme = useTheme()` to Layout.jsx, but the edit
that inserts the button matched a string with different indentation than the
file actually had, so it silently did nothing. The import landed, the button
did not, and `theme` sat unused — which lint does not flag, because a variable
that is assigned and never read is only a warning and the value IS technically
used by the assignment.

Verified this time by building a clean checkout and grepping the OUTPUT for the
Hebrew label, rather than checking the source and assuming.

FIXED
  Layout.jsx   the toggle now exists, and is LABELLED ("🌙 מצב Neat") rather
               than a bare icon. A lone moon glyph among four other buttons is
               not discoverable — you looked for it and could not find it.
  Login.jsx    a toggle there too; that screen has no topbar, so the theme was
               unreachable before signing in.
  index.css    clickable row names now carry a permanent dotted underline.
               They relied on hover, which does not exist on a phone, so there
               was no sign at all that a row could be opened.

After deploying, index.html should reference index-CDp9qYhk.js. If it still
shows the old name, the Action failed — check the Actions tab.
